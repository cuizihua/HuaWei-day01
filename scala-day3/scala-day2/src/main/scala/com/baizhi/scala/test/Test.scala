package com.baizhi.scala.test

import java.util.Date

//import com.baizhi.scala.Emp
//import com.baizhi.scala.entity.{Person, Student}
//import com.baizhi.scala.service.{S, StudentService}
//import com.baizhi.scala.service.impl.StudentServiceImpl

object Test {

  /*def main(args: Array[String]): Unit = {
    var person2:Person=new Person//通过new关键字创建对象

    //scala推荐使用val创建对象
    val person1 = new Person(12)

    var person:Person=new Student

    if(person.isInstanceOf[Student]){//判断person是否时Student类型
      var student:Student=person.asInstanceOf[Student]//

    }

    //动态混入：
    //在创建对象的时候，实现一个接口，同时实现里面的方法
    var studentService=new StudentServiceImpl() with S{
      override def a(): Unit = {

        println("*************")
      }
    }

    studentService.a()//调用方法
    //在使用动态混入的时候，通过类型推导，让编译器指定对象的类型

    var emp:Emp=new Emp("张三",22,new Date())

    emp.name

  }*/

}
